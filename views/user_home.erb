<html>
<head>
</head>
    <style>
        header {
            border-bottom: 5px solid white;
            border-bottom-style: ridge;
            height: 45px;
        }
        h1 {
            display: inline-block;
            font-family:Helvetica;
            font-weight: bolder;
            margin: 0;
            padding: 0;
            line-height: 57px;
        }
        .form {
            margin: 0;
            padding: 0;
            float: right;
            display: inline-block;
            line-height: 62px;
        }
        #post{
            background-color: white;
            color: black;
            font-family: Helvetica;
            font-weight: bold;
            font-size: 20px;
            border: 1px solid #000000;
            transition-duration: 0.4s;
            -webkit-transition-duration: 0.4s;}
        #post:hover{
            background-color: #000000;
            color: white;
            border: 1px solid white;
        }    
        #settings{
            background-color: white;
            color: black;
            font-family: Helvetica;
            font-weight: bold;
            font-size: 20px;
            border: 1px solid #000000;
            transition-duration: 0.6s;
            -webkit-transition-duration: 0.6s;
        }
        #settings:hover{
            background-color: #000000;
            color: white;
            border: 1px solid white;
        }
        #post_image{
            background-color: white;
            color: black;
            font-family: Helvetica;
            font-weight: bold;
            font-size: 20px;
            border: 1px solid #000000;
            transition-duration: 0.6s;
            -webkit-transition-duration: 0.6s;
        }
        #post_image:hover{
            background-color: #000000;
            color: white;
            border: 1px solid white;
        }
        #logout{
            background-color: white;
            color: black;
            font-family: Helvetica;
            font-weight: bold;
            font-size: 20px;
            border: 1px solid #000000;
            transition-duration: 0.6s;
            -webkit-transition-duration: 0.6s;
        }
        #logout:hover{background-color: #000000;
            color: white;
            border: 1px solid white;}
ul.pagination{display: inline-block;
    padding: 0;
    margin: 0;}
ul.pagination li{display: inline;}
ul.pagination li a {
    color: black;
    float: left;
    padding: 8px 16px;
    text-decoration: none;}
ul.pagination li a.active {
    background-color: #4CAF50;
    color: white;}
ul.pagination li a:hover:not(.active) {background-color: #ddd;}
    </style>
<body>
    <header>
        <h1>The American Voice</h1>
        <form class="form" method="post">
        <input name="search_query" type="text" id="search_query"><input name="search" type="submit" id="search">
        <% if @privilege > 0 %>
            <input name="post" type="submit" value="Post Text" id="post">
            <input name="post_image" type="submit" value="Post Image" id="post_image">
        <% end %>
            <input name="settings" type="submit" value="Settings" id="settings">
            <input name="logout" type="submit" value="Logout" id="logout">

        </form>
    </header>

    <% for i in 0...@links_on_page[@page.to_i].length %>
        <p>
            <%= @types_on_page[@page.to_i][i].capitalize %> Post: <a href=<%=@links_on_page[@page.to_i][i]%>><%=@titles_on_page[@page.to_i][i]%></a>
        </p>
    <% end %>

    <ul class="pagination">
    <% for i in (0...@pages.to_i) %>
        <li><a href=<%="?page=#{i.to_s}"%>><%=(i+1).to_s%></a></li>
    <% end %> 
    </ul>
</body>
</html>